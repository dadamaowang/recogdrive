
https://my.feishu.cn/docx/K704dPFpNoYbvjxUNwvcGXFxn9d#share-XmE7dmvdnoO6ESxPUoJcrJlunof


CUDA 11.8, python 3.9 

1. 设置 python 3.9
conda create -n nd python=3.9

2. 按照 recogdrive 的 requirements 一条条搞 (一律 pip 装)（https://github.com/dadamaowang/recogdrive/blob/main/requirements.txt）

2.1
# nuplan requirements
aioboto3
aiofiles
bokeh==2.4.3  # Used in the nuBoard dashboard
control==0.9.1  # Used for LQR controller synthesis
Fiona  # Used in GpkgMapsDB.py
geopandas>=0.12.1  # Used to read maps
guppy3==3.1.2
hydra-core==1.2.0  # Used for configs
joblib
matplotlib  # Used for rendering
nest_asyncio  # Used to nest event loops when running nuBoard/jupyter
numpy==1.26.4
opencv-python==4.9.0.80  # Used widely
pandas    # Used widely
Pillow    # Used widely to render images
psutil  # Used widely to get the number of CPUs
pyarrow # For parquet
pyinstrument    # Used widely as profiler
pyogrio # A performant backend for geopandas
pyquaternion>=0.9.5  # Used widely, avoid known bug with quaternion slerp
rasterio  # Used in GpkgMapsDB.py
ray # Used widely
retry
rtree # Used in occupancy maps
scipy  # Used widely
selenium # Used in bokeh export png
setuptools==65.5.1  # Used in setup.py, pinned to not break pytorch
Shapely>=2.0.0  # Used widely
SQLAlchemy==1.4.27  # older versions don't work with some table definitions
sympy  # Use for symbolic algebra
tornado  # Used in nuboard.py
tqdm  # Used widely
ujson  # Used in serialiation_callback.py
diffusers

2.2
pip install positional-encodings==6.0.1

2.3
下边的逐一装

torch
torchvision
pytorch-lightning
tensorboard==2.16.2
protobuf==4.25.3

notebook
timm

3.

 -e . 

3.1 装 navism:进入自己的 recogdrive ， pip install -e .（requirements.txt 弄空）
(新添加的 agent, 写个 __init__.py ，然后 pip install -e .；不然启动脚本的时候 agent 加载不进来)

3.2 装 nuplan: https://github.com/motional/nuplan-devkit/


4. 微调 LLM 相关 
https://github.com/dadamaowang/recogdrive/blob/main/internvl_chat/internvl_chat.txt
都装上


5. 重整 peft 相关包的匹配性
pip install "peft>=0.17.0" "transformers>=4.45.0" "accelerate>=1.0.0"


trouble shooting

ImportError: libGL.so.1: cannot open shared object file: No such file or directory

1. 装 OpenGL lib
sudo apt update
sudo apt install -y libgl1

2. 或者替换，用 headles (九章成功过）（总之就是避免 GUI dependencies ）
pip uninstall opencv-python opencv-python-headless -y
pip install opencv-python-headless  # Use headless if no GUI needed


future 问题

[rank0]:     ) -> torch.Tensor | tuple[torch.Tensor, List[torch.Tensor]]:
[rank0]: TypeError: unsupported operand type(s) for |: 'torch._C._TensorMeta' and 'types.GenericAlias'

必要文件前加 
from future import annotations  # python 3.9 版本较旧问题




---



以上都做完，是可以跑成功的